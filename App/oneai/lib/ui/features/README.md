# Features Directory

This directory contains feature-specific UI implementations:

- Each subdirectory represents a specific feature or screen in the application
- Features typically include:
  - View files: Screen implementations
  - `view_model/`: BLoC/ViewModel implementations for state management
  - `widgets/`: Feature-specific UI components

This modular structure promotes separation of concerns and easier maintenance of feature-specific code.