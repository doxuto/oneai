# Theme View Models

This directory contains view models for theme management:

- BLoC implementations for theme state management
- Theme-related events and states
- Theme persistence and selection logic

These view models handle the business logic for theme selection and application.