# Core UI Components

This directory contains reusable UI components:

- Shared widgets
- Common UI elements
- Animation utilities

These components provide consistent UI building blocks that can be used across different features.