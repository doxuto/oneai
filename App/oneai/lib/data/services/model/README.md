# Model Services

This directory contains data model services:

- Data transfer objects (DTOs) used for API requests and responses
- Serialization/deserialization logic for data models
- Mapping functions between external data formats and domain models

These services handle data transformation between external sources and the domain layer.